  [DONE] Job states in Redis — status transitions (waiting/active/completed/failed) persisted in Redis with timestamps
  [DONE] Delayed jobs — Redis sorted set scored by Unix timestamp, scheduler goroutine polls every second and moves ready jobs to main queue
  [DONE] Retry logic with exponential backoff — fault tolerance, highest resume impact for CockroachDB
  [DONE] Dead letter queue — jobs that exhaust retries land here, shows durability thinking
  [DONE] Benchmarks — enqueue/dequeue load tests to get jobs/sec number
  [DONE] Unit tests — TestEnqueue, TestDequeue, TestRetry, TestDeadLetter, TestNoHandler
  [DONE] Job routing by name — Register(name, fn) dispatches jobs to the right handler by job.Name

  [DONE] Dead-letter inspection — ListDead() and RetryDead() to see and requeue failed jobs
  [DONE] Priorities — urgent jobs jump ahead of normal ones in the queue
  [DONE] TestPriority — enqueue high and low priority jobs, verify high comes out first
  [NEXT] Stalled job detection — if a worker crashes mid-job, reassign it instead of losing it
  [NEXT] Rate limiting — max N jobs per second so you don't overwhelm external services
  [NEXT] Repeatable jobs — cron-like scheduling (run every hour, every day, etc.)

  - Pause/resume — stop a queue without killing workers
  - Job progress — workers can report % completion
  - Job results storage — completed job data persisted in Redis
  - Events/pub-sub — real-time job updates via Redis pub/sub
  - Library refactor — move from package main to package gqueue (do this last)